{
     "name": "bridge",
     "type": "bridge",
     "bridge": "kube-br",
     "isGateway": true,
     "isDefaultGateway": true,
     "ipMasq": false,
     "forceAddress": true,
     "ipam": {
         "type": "host-local",
         "subnet": "{{ podnet_cidr }}"
     }
}
